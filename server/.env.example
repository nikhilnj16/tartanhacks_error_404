# Firebase / Firestore (required for DB)
# Option A: path to your Firebase service account JSON file (no space after =)
# GOOGLE_APPLICATION_CREDENTIALS=./tartanhacks-26-error404-firebase-adminsdk-fbsvc-f06976fc05.json

# Option B: paste the full JSON as a single line (e.g. in production)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# If unset, the app looks for any *adminsdk*.json in the server folder.

# JWT for auth (optional; set in production)
# JWT_SECRET=your-secret-key-change-in-production
